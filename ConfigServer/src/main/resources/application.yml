server:
  port: 8888

spring:
  application:
    name: config-server-git
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/er3nnz/ConfigServer-YML.git"
          default-label: "main"
          skip-ssl-validation: true
          username: "er3nnz"
          password: ${GIT_KEY}